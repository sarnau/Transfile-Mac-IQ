/*** *	WindowsGlobal.h * *	©1992 ·-Soft, Markus Fritze ***/#pragma once	// include this header				// only once per source fileWORD		WindowTHeight(REG WindowPtr w);VOID		BringBehind(REG WindowPtr macWindow,WindowPtr behindWindow);Boolean		IsAppWindow(REG WindowPtr window);Boolean		IsDAWindow(REG WindowPtr window);Boolean		IsDialogWindow(REG WindowPeek window);VOID		ZoomToWindowDevice(REG WindowPtr window,LONG maxWidth,LONG maxHeight,WORD zoomDir,Boolean front);