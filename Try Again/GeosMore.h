#pragma once	// include this header				// only once per source fileextern int			gMausx,gMausy;	// Mauspositionextern int			mx,my;			// Mauspositionextern int			button;			// Maustasten-Statusextern int			checkbutton;	// Maustasten-Status mit ÒlŠngerem Maus-DownÓextern int			keystate;		// Kbshift-Statusextern int			keyscan,keyascii; // Scancode & ASCII-Code der aktuellen Taste#define CheckKey(theKeyCode) (((UBYTE*)&(theKeys))[(theKeyCode)>>3] & (1<<((theKeyCode) & 7)))void	SetupGVars(EventRecord *event);short	vqkey(void);short	vqmouse(void);short	alert(short defbut,char *str);short	CheckResource(Handle r);short	CheckAllocation(void *p);Boolean	CheckMem(LONG bytes);short	SevereMacError(REG OSErr macErr);void	SysErrRecover(void);Boolean	CheckOSError(REG OSErr macErr);void	JumpToEventLoop(void);