/*** *	Business.c ***/#include "GlobalDefines.h"#include "Windows.h"#include "xRsrcDefines.h"#include "Geos.h"#include "Memo.h"#include "Telephone.h"#include "Business.h"/*** *	Window öffnen ***/OSErr	OpenBusinessWindows(void)		/* Window für Businesscard öffnen */{	REG WindPtr	w1;	w1 = MyOpenWind(FULLER | (1L<<noOrigin),WindBusiness,strBusiness,BUSINESS);	/* Window mit Slidern */	if(!w1) 		return(memFullErr);				// Fehler beim Öffnen 	MultiOpen(w1,BUSINESS);		WFUNC(w1,menuupdate,telupdate);		// Darstellung enabeln etc.	return(noErr);						// alles ok!}