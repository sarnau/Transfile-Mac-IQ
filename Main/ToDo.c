/*** *	ToDo.c ***/#include "GlobalDefines.h"#include "Windows.h"#include "xRsrcDefines.h"#include "Memo.h"#include "Telephone.h"#include "ToDo.h"/*** *	Window öffnen ***/OSErr	OpenToDoWindows(void)		/* Window für Aufgaben öffnen */{	REG WindPtr	w1;	w1 = MyOpenWind(FULLER | (1L<<noOrigin),WindToDo,strToDo,TODO);	/* Window mit Slidern */	if(!w1) 		return(memFullErr);				// Fehler beim Öffnen 	MultiOpen(w1,TODO);	WFUNC(w1,menuupdate,telupdate);		// Darstellung enabeln	return(noErr);						// alles ok!}