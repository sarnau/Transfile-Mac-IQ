////	MyPrint.h//#pragma once	// include this header only once per source fileextern short		columnsPerPage;			// Zahl der Spaltenextern Boolean		fieldNames;				// true: Feldnamen werden mitgedrucktextern Boolean		printHeader;			// true: Seitenheader wird gedruckt#include <Packages.h>#define	MAX_PAGES				2000	// Denn so gro§ ist das Array nur#define MAX_COLUMNS_PER_PAGE	8		// Maximale Spaltenanzahl pro Seitevoid	PrintDialog(short type,DialogPtr d,short item);void	DoPrint(TPPrPort p, THPrint print);void	PrintHeader(unsigned long dateTime, DateForm temp, Str255 s);short	PutDataOnPages(TPPrPort p,IQFileType type, THPrint print);