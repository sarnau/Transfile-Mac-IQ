#pragma once	// include this header				// only once per source fileextern short		gAppleId,gFileId,gEditId,gWindowsId; // Menü-IDsvoid	MenuBarInit(short MenuId);				// Menüleiste initialisierenBoolean	GetMenuAble(MenuHandle m,short item);	// Menüpunkt enabled bzw. disabled?VOID	MenuAble(WORD menuid,CHAR *id);			// mehrere Menüpunkte enablen/disablenvoid	AdjustMenus(void);						// Menüpunkt enablen bzw. disablenvoid	DoMenuCommand(long m);					// Menüpunkt angewählt